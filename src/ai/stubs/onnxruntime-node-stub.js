/**
 * Stub for onnxruntime-node in browser environment
 * @xenova/transformers imports both node and web versions,
 * this stub prevents errors when the node version is not available
 */

// Export empty object - transformers.js checks for existence and falls back to web version
export default {};
